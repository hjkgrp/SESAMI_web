runtime: python310 # or another supported version

instance_class: F2

env_variables:
  BUCKET_NAME: "example-gcs-bucket"
  MONGODB_URI: "mongodb+srv://iast:7nqdtF3aI2n5mBC9@cluster0-pri.jadjk.mongodb.net/data_isotherm?retryWrites=true&w=majority"

handlers:
# Matches requests to /images/... to files in static/images/...
- url: /images
  static_dir: static/images

- url: /.*
  secure: always
  redirect_http_response_code: 301
  script: auto
